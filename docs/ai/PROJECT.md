# ops-hub — Project compatibility note

この文書は過去taskからのlink互換性のためだけに残す。AIの共通ルールは`AGENTS.md`、task固有の契約は`docs/task-ID-*.md`、機械生成された現在地は`docs/task-INDEX.md`を参照する。

長期ロードマップは`docs/implementation-plan.md`、長期的な設計理由は`docs/adr/`に置く。通常の実装ではこれらを全文再読せず、承認済みtaskのFilesから開始する。
